{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Visualizesse": "Host=localhost;Username=postgres;Password=docker;Database=postgres;Include Error Detail=true"
  }
}

// docker run --name visualizesse -e POSTGRES_PASSWORD=docker -p 5432:5432 -d postgres

// docker run --name rusttodoapp -e POSTGRES_USER=postgresrustapp -e POSTGRES_PASSWORD=docker -e POSTGRES_DB=postgres -p 5432:5432 -d postgres